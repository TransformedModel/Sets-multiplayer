{
  "name": "online-set-multiplayer-game",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev:server": "node server/index.js",
    "dev:client": "npm run dev --prefix client",
    "dev": "npm-run-all --parallel dev:server dev:client",
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "node server/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Multiplayer Set (Express + WebSocket + Vite). Deploy on Render: build npm ci && npm run build, start npm start (see render.yaml).",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "sharp": "^0.34.5",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
